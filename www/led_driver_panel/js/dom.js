const entrySelect = document.getElementById("entry-select");
const loadButton = document.getElementById("load");
const refreshButton = document.getElementById("refresh");
const statusEl = document.getElementById("status");
const errorEl = document.getElementById("error");
const dashboardContainer = document.getElementById("dashboard");
const groupsContainer = document.getElementById("groups");
const switchesContainer = document.getElementById("switches");
const buttonDiscoveriesContainer = document.getElementById("button-discoveries");
const controllersContainer = document.getElementById("controllers");
const controllerMapContainer = document.getElementById("controller-map");
const driversContainer = document.getElementById("drivers");
const groupDefsContainer = document.getElementById("group-defs");
const patchPanelVisual = document.getElementById("patch-panel-visual");
const patchPanelPortsContainer = document.getElementById("patch-panel-ports");
const patchPanelTooltip = document.getElementById("patch-panel-tooltip");
const patchPanelDetails = document.getElementById("patch-panel-details");
const switchVisualContainer = document.getElementById("switch-visual");
const switchVisualSelect = document.getElementById("switch-visual-select");
const ssrContainer = document.getElementById("ssr-panel");
const acsContainer = document.getElementById("acs");
const serialLogContainer = document.getElementById("serial-log");
const addControllerButton = document.getElementById("add-controller");
const addDriverButton = document.getElementById("add-driver");
const addGroupButton = document.getElementById("add-group");
const addSwitchButton = document.getElementById("add-switch");
const addSsrButton = document.getElementById("add-ssr");

export {
  entrySelect,
  loadButton,
  refreshButton,
  statusEl,
  errorEl,
  dashboardContainer,
  groupsContainer,
  switchesContainer,
  buttonDiscoveriesContainer,
  controllersContainer,
  controllerMapContainer,
  driversContainer,
  groupDefsContainer,
  patchPanelVisual,
  patchPanelPortsContainer,
  patchPanelTooltip,
  patchPanelDetails,
  switchVisualContainer,
  switchVisualSelect,
  ssrContainer,
  acsContainer,
  serialLogContainer,
  addControllerButton,
  addDriverButton,
  addGroupButton,
  addSwitchButton,
  addSsrButton,
};
